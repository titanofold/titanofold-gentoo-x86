# Space separated list of NTP servers
SERVERS="0.gentoo.pool.ntp.org 1.gentoo.pool.ntp.org \
         2.gentoo.pool.ntp.org 3.gentoo.pool.ntp.org"

# While not necessary, any bugs you may need to report will need an
# associated tracefile.
#TRACEFILE="/var/log/ntimed-client.trace"

# You shouldn't need to set this as the defaults are just about as
# good as they get. See the output of `ntimed-client -p '?'` for a
# list of valid parameters.
#PARAMETERS="-p 'foo=bar'"

