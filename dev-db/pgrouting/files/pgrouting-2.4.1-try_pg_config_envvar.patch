--- a/cmake/FindPostgreSQL.cmake	2017-03-24 10:26:27.000000000 -0400
+++ b/cmake/FindPostgreSQL.cmake	2017-04-22 09:24:59.041011900 -0400
@@ -30,11 +30,15 @@
             NO_DEFAULT_PATH
             )
     else(NOT "${POSTGRESQL_BIN}" STREQUAL "")
+      if($ENV{PG_CONFIG})
+        set(POSTGRESQL_PG_CONFIG $ENV{PG_CONFIG})
+      else($ENV{PG_CONFIG})
         # Checking POSTGRESQL_PG_CONFIG 
         find_program(POSTGRESQL_PG_CONFIG NAMES pg_config
             PATHS
             /usr/lib/postgresql/*/bin/
             )
+      endif($ENV{PG_CONFIG})
     endif(NOT "${POSTGRESQL_BIN}" STREQUAL "")
 
     message(STATUS "POSTGRESQL_PG_CONFIG is " ${POSTGRESQL_PG_CONFIG})
